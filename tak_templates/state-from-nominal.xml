<?xml version="1.0" encoding="UTF-8"?>
<state id="{ID}" name="{TAK_NAME}" concept-type="state">
  <categories/>
  <standard-terms/>
  <synonyms/>
  <temporal-semantic downward-hereditary="{TEMPORAL_downward-hereditary}" forward="{TEMPORAL_forward}" backward="{TEMPORAL_backward}" solid="{TEMPORAL_solid}" concatenable="{TEMPORAL_concatenable}" gestalt="{TEMPORAL_gestalt}"/>
  <clippers/>
  <derived-from>
    <derived-from-id>{DERIVED_FROM}</derived-from-id>
  </derived-from>

  <abstraction-at-contexts>
    <abstraction-at-context>
      <at-context>
        <necessary-contexts>
          <necessary-context-id>{DERIVED_FROM}</necessary-context-id>
        </necessary-contexts>
        <exclusion-contexts/>
      </at-context>
      <state-at-context id="{ID}" name="{TAK_NAME}" concept-type="state">
        <categories/>
        <standard-terms/>
        <synonyms/>
        <temporal-semantic downward-hereditary="{TEMPORAL_downward-hereditary}" forward="{TEMPORAL_forward}" backward="{TEMPORAL_backward}" solid="{TEMPORAL_solid}" concatenable="{TEMPORAL_concatenable}" gestalt="{TEMPORAL_gestalt}"/>
        <clippers/>
        <derived-from>
          <derived-from-id>{DERIVED_FROM}</derived-from-id>
        </derived-from>
        <abstraction-at-contexts/>

        <mapping-function rank-selection-criteria="{Mapping_Rank_Selection_Criteria}">
          <mapping-functions-to-values>
            <!-- 
            Each block maps a **nominal value** to a corresponding **ordinal label**
            → STATE_LABELS[idx] = Ordinal label you want to show
            → RAW_VALUES[idx] = Nominal value appearing in the raw concept
            ✔ Ensure the value exists in the raw concept definition
            ✔ Keep mapping-function-2-value blocks in increasing order
            -->
            <mapping-function-2-value order="0" value="{STATE_LABELS[0]}">
              <evaluation-tree data-type="comparison-function">
                <comparison-function comparison-operator="equal">
                  <left data-type="concept-id-allowed-values">
                    <concept-id-allowed-values id="{DERIVED_FROM}"/>
                  </left>
                  <right data-type="string">
                    <string>{RAW_VALUES[0]}</string>
                  </right>
                </comparison-function>
              </evaluation-tree>
            </mapping-function-2-value>
            <!-- Duplicate this block for each additional value: -->
            <!-- order="1", value="{STATE_LABELS[1]}" with string "{RAW_VALUES[1]}" -->
          </mapping-functions-to-values>
        </mapping-function>

        <ordinal-allowed-values output-type="ordinal">
          <persistence>
            <global-persistence granularity="second" behavior="pos-pos">
              <interpolation-table>
                <rows/>
              </interpolation-table>
            </global-persistence>
            <local-persistence>
              <good-before value="{LOCAL_PERSISTENCE_GOOD_BEFORE_VALUE}" granularity="{LOCAL_PERSISTENCE_GOOD_BEFORE_GRANULARITY}"/>
              <good-after value="{LOCAL_PERSISTENCE_GOOD_AFTER_VALUE}" granularity="{LOCAL_PERSISTENCE_GOOD_AFTER_GRANULARITY}"/>
            </local-persistence>
          </persistence>
          <values>
            <ordinal-allowed-value order="0" value="{STATE_LABELS[0]}"/>
            <!-- Add more values as needed, incrementing order -->
          </values>
        </ordinal-allowed-values>
      </state-at-context>
    </abstraction-at-context>
  </abstraction-at-contexts>

  <!-- Required by schema but unused -->
  <mapping-function>
    <mapping-functions-to-values/>
  </mapping-function>

  <ordinal-allowed-values output-type="ordinal">
    <persistence>
      <global-persistence granularity="second" behavior="pos-pos">
        <interpolation-table>
          <rows/>
        </interpolation-table>
      </global-persistence>
      <local-persistence>
        <good-before value="{LOCAL_PERSISTENCE_GOOD_BEFORE_VALUE}" granularity="{LOCAL_PERSISTENCE_GOOD_BEFORE_GRANULARITY}"/>
        <good-after value="{LOCAL_PERSISTENCE_GOOD_AFTER_VALUE}" granularity="{LOCAL_PERSISTENCE_GOOD_AFTER_GRANULARITY}"/>
      </local-persistence>
    </persistence>
    <values>
      <ordinal-allowed-value order="0" value="{STATE_LABELS[0]}"/>
      <!-- Same STATE_LABELS as above -->
    </values>
  </ordinal-allowed-values>
</state>